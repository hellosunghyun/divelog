# Task 5: Response/Sentence Generation Action Draft Check + getRecordBySlug Defense-in-Depth

## Completion Status: ✅ COMPLETE

### Changes Made

#### 1. getRecordBySlug() in app/db/queries/records.server.ts (lines 63-87)
- Added optional `currentUserId?: string` parameter
- Added defense-in-depth check: if record is draft and currentUserId is provided but doesn't match authorId, return null
- Backward compatible: currentUserId is optional, all existing callers still work

#### 2. create_response action in app/routes/public/logs/$recordSlug.tsx (lines 169-184)
- Extended query to select visibility and authorId in addition to responsePreference
- Added draft visibility check: if record is draft and user is not author, return error "이 기록에 응답할 수 없습니다."
- Check happens BEFORE responsePreference validation (defense-in-depth)

#### 3. save_sentence action in app/routes/public/logs/$recordSlug.tsx (lines 211-239)
- Added query to fetch target record's visibility and authorId
- Added draft visibility check: if record is draft and user is not author, return error "이 기록에 문장을 저장할 수 없습니다."
- Check happens BEFORE saveSentence() call

### Verification Results

#### Test Results
✅ npm run test: 78/78 tests PASSED
- Test Files: 6 passed
- Tests: 78 passed
- Duration: 258ms

#### Build Results
✅ npm run build: SUCCESS
- Client build: ✓ 2629 modules transformed
- Server build: ✓ 2757 modules transformed
- Worker bundle: 3.4mb
- Build completed in 92ms

#### Draft Check Verification (grep)
✅ All 3 locations have draft visibility checks:

1. app/db/queries/records.server.ts:83
   if (recordData && recordData.record.visibility === "draft" && currentUserId && currentUserId !== recordData.record.authorId)

2. app/routes/public/logs/$recordSlug.tsx:182
   if (visibility === "draft" && authorId !== auth.user.id)
   [create_response action]

3. app/routes/public/logs/$recordSlug.tsx:241
   if (visibility === "draft" && authorId !== auth.user.id)
   [save_sentence action]

### Defense-in-Depth Layers

1. **Route Loader** (line 78-81): Existing check prevents draft records from being loaded for non-authors
2. **create_response Action** (line 182-184): New check prevents responses on draft records
3. **save_sentence Action** (line 241-243): New check prevents sentence saves on draft records
4. **getRecordBySlug()** (line 83-85): New optional check for programmatic access

### Issues Fixed
- ✅ C-1: getRecordBySlug() now has visibility filter (defense-in-depth)
- ✅ C-7: Response creation action now verifies target record is not draft
- ✅ H-9: save_sentence action now verifies target record is not draft

### Notes
- All error messages use Korean as per AGENTS.md language requirement
- Comments added to explain security-critical defense-in-depth checks
- No changes to schema.server.ts or auth files
- No changes to existing draft check in route loader (kept for defense-in-depth)
- self_answer action already correctly checks authorId (no changes needed)
