# Task 37: Admin Dashboard 시각 업그레이드

## 변경 사항

### app/routes/admin/index.tsx

1. **통계 카드 dense 레이아웃**
   - `gap-5` → `gap-4` (간격 축소)
   - `p-5` → `p-4` (패딩 축소)
   - `tabular-nums` 추가 (숫자 정렬)
   - 개별 카드 구조로 변경 (map 제거)

2. **최근 기록 테이블 변환**
   - 기존: flex 기반 리스트
   - 변경: `<table>` 요소 사용
   - `thead`/`tbody` 구조
   - `hover:bg-admin-surface-secondary` 행 hover 효과
   - `uppercase` 헤더 텍스트
   - `tabular-nums` 날짜 컬럼

3. **Flagged 기록 테이블**
   - 기존: flex 기반 리스트
   - 변경: `<table>` 요소 사용
   - 에러 강조 헤더 (`bg-red-50/50`)

4. **빈 상태 처리**
   - `recentRecords.length === 0` 시 "기록이 없습니다" 메시지

5. **타입 정의 추가**
   - `FlaggedRecord` 타입
   - `RecentRecord` 타입

## 규칙 준수

- ✅ Double-Bezel 미사용 (단순 `border` 사용)
- ✅ spring physics 미적용
- ✅ staggered entry 미적용
- ✅ loader 변경 없음
- ✅ `tsc --noEmit` 0 errors

## 디자인 원칙

Admin은 Public과 다른 톤: neutral, calm, efficient, dense, readable
- 여백: compact
- radius: 20~24px → 약간 줄임
- 정보 밀도: 높음
- dense table 허용
