# T19 Evidence: /learners 페이지 강화

## 완료 항목

### 1. getLearnersWithActivity 쿼리 추가
**파일**: `app/db/queries/learners.server.ts`

- `getLearnersWithActivity(d1, cohort?)` 함수 추가
  - learner_profiles 전체 조회 (cohort 필터 선택적)
  - 각 learner의 최근 기록 조회 (slug, title, createdAt)
  - 현재 stage 정보 조회 (currentStageId 기반)
  - 최근 활동 시간 기준 정렬 (활동 없는 learner는 마지막)
  - LearnerCard에 필요한 모든 데이터 반환

- `getDistinctCohorts(d1)` 함수 추가
  - 필터링용 cohort 목록 조회

### 2. _public.learners._index.tsx 완전 구현
**파일**: `app/routes/_public.learners._index.tsx` (35줄 → 113줄)

**Loader 기능**:
- URL searchParam `?cohort=` 처리
- getLearnersWithActivity 호출로 모든 데이터 한 번에 조회
- learners, cohorts, selectedCohort 반환

**UI 기능**:
- HeroSection (variant="learner")
- Cohort 필터 드롭다운 (cohort 존재 시에만 표시)
- 반응형 그리드: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3`
- LearnerCard 사용 (질문이 프로필보다 먼저 보임)
  - learner: userId, slug, displayName, profilePhotoUrl, cohort, bio, currentQuestion
  - recentRecord: slug, title (최근 기록)
  - stage: name (현재 stage)
- EmptyState 사용 (learners variant + cohort 필터 시 커스텀 메시지)

### 3. 검증 결과

```
$ npx tsc --noEmit
(에러 없음)

$ pnpm build
✓ built in 814ms (client)
✓ built in 726ms (ssr)
```

## Must NOT DO 준수 확인

- [x] 랭킹/비교 표시 없음 ("가장 활발한 Learner" 등)
- [x] 기록 수 기반 정렬 없음 (활동 시간 기반만)
- [x] 팔로우/구독 기능 없음
- [x] 총 기록 수를 카드에 크게 표시하지 않음
- [x] `as any`, `@ts-ignore` 사용 없음

## 디자인 요구사항 준수

- [x] 질문이 프로필(아바타)보다 먼저 보임 (LearnerCard DOM 순서)
- [x] 아바타는 보조적 역할
- [x] Quiet Depth 디자인 (bg-surface, border-border, rounded-2xl)
- [x] 반응형: 모바일 1열, 태블릿 2열, 데스크톱 3열
