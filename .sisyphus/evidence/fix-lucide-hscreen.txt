# lucide-react 완전 제거 + h-screen → min-h-screen 수정

## 작업 완료 내역

### 1. lucide-react → Phosphor Icons 교체 (4개 파일)

#### app/components/ui/select.tsx
- ✅ Check → Check (weight="light")
- ✅ ChevronDown → CaretDown (weight="light") [3개 위치]
- ✅ ChevronUp → CaretUp (weight="light")

#### app/components/ui/checkbox.tsx
- ✅ Check → Check (weight="light")

#### app/components/ui/dialog.tsx
- ✅ X → X (weight="light")

#### app/components/ui/radio-group.tsx
- ✅ Circle → Circle (weight="fill")

### 2. h-screen → min-h-screen 수정

#### app/routes/_admin.tsx:39
- ✅ <div className="flex h-screen overflow-hidden bg-admin-bg">
- ✅ → <div className="flex min-h-screen overflow-hidden bg-admin-bg">

### 3. 패키지 제거

```bash
pnpm remove lucide-react
```
✅ 성공 (1 package removed)

### 4. 검증

#### grep 검증
```bash
grep -r "lucide-react" app/
```
✅ 0 결과 (완전 제거됨)

```bash
grep -r "h-screen" app/
```
✅ 0 결과 (모두 min-h-screen으로 변경됨)

#### TypeScript 검증
```bash
pnpm tsc --noEmit
```
✅ 0 errors (모든 타입 체크 통과)

## 결론

- lucide-react 완전 제거 ✅
- Phosphor Icons로 모두 교체 ✅
- h-screen → min-h-screen 수정 ✅
- 타입 체크 통과 ✅
- 준비 완료: git commit
