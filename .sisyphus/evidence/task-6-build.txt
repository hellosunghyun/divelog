# Task 6: /me 페이지 draft 중복 표시 해결 (H-1)

## Changes Made

### 1. Import `ne` from drizzle-orm (Line 5)
- Added `ne` to the import statement
- Before: `import { eq, and, desc, sql, asc } from "drizzle-orm";`
- After: `import { eq, and, desc, sql, asc, ne } from "drizzle-orm";`

### 2. Fixed myRecordsWithStage query (Line 83)
- Added visibility filter to exclude drafts from "나의 여정" section
- Before: `.where(eq(records.authorId, auth.user.id))`
- After: `.where(and(eq(records.authorId, auth.user.id), ne(records.visibility, "draft")))`

### 3. Removed draft badge from journey section (Lines 205-207)
- Removed conditional rendering of "임시저장" badge in the journey section
- Before:
  ```
  <h4 className="text-base font-medium text-text-primary mb-1">
    {record.title}
    {record.visibility === "draft" && (
      <span className="ml-2 text-caption text-warning font-medium">임시저장</span>
    )}
  </h4>
  ```
- After:
  ```
  <h4 className="text-base font-medium text-text-primary mb-1">
    {record.title}
  </h4>
  ```

## Verification

✅ npm run test: 78/78 tests passing
✅ npm run build: Build succeeded
✅ No TypeScript errors
✅ Draft records no longer appear in "나의 여정" section
✅ Draft records still appear in "임시저장" section (unchanged)

## Impact

- Fixes H-1: Draft records no longer show twice on /me page
- "나의 여정" section now only shows published/cohort records
- "임시저장" section continues to show all draft records
- No breaking changes to other functionality
