# Issues — logs-timeline-calendar

## 알려진 이슈
- CSS LSP 에러 (global.css, app.css) — Tailwind v4 신택스. 프리-이그지스팅, 무시
- Seed data: 25개 중 23개가 stage-challenge-1 집중. QA 시 유의
- @radix-ui/react-popover 미설치 → T2에서 처리

## 가드레일 체크리스트
- [ ] groupRecordsByDate() 수정 금지
- [ ] SceneCard.tsx 수정 금지
- [ ] framer-motion 직접 import 금지
- [ ] 기존 URL 파라미터 호환 유지

## Task 4: Verification blocker
- `pnpm typecheck` fails from pre-existing unrelated files, including `app/components/admin/AdminSidebar.tsx`, multiple `app/routes/admin/*.tsx`, `app/db/queries/dialogue/*.server.ts`, and `app/routes/api/__tests__/autosave.test.ts`; calendar utility files themselves have clean LSP diagnostics
